spring:
  application:
    name: aston-gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true
      retry:
        initial-interval: 2000
        max-attempts: 10

eureka:
  client:
    service-url:
      defaultZone: http://discovery:8761/eureka/